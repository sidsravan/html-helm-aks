apiVersion: v1
kind: Service
metadata:
  name: {{ include "akschart.fullname" . }}
  labels:
    {{- include "akschart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "akschart.selectorLabels" . | nindent 4 }}
