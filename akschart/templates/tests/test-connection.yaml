apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "akschart.fullname" . }}-test-connection"
  labels:
    {{- include "akschart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "akschart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
